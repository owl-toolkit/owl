image: gitlab-runner/owl

before_script:
  - chmod +x gradlew

stages:
  - build
  - test
  - test-optimizations
  - test-parallel-translation
  - bench

build:
  stage:
    build
  script:
    - ./gradlew --no-daemon build
  artifacts:
    paths:
    - build/reports/
    when: on_failure
    expire_in: 1 week

.ltlcross: &ltlcross_template
  script:
    - ./gradlew --no-daemon buildBin
    - LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$(pwd)/src/main/resources python3 scripts/util.py test $TEST_NAME
  artifacts:
    paths:
    - build/results/
    when: always
    expire_in: 1 week
  dependencies: []

.benchmark: &benchmark_template
  stage: bench
  script:
    - LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$(pwd)/src/main/resources python3 scripts/util.py bench $BENCH_NAME | tee benchmark.txt
  artifacts:
    paths:
    - benchmark.txt
    when: always
    expire_in: 1 week
  when: manual
  dependencies: []

# LTL to LDBA Translations

## Breakpoint

ltl2ldba Büchi:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2ldba"
  <<: *ltlcross_template

ltl2ldba Büchi Non-Det:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2ldba-nondet"
  <<: *ltlcross_template

ltl2ldba Büchi Vanilla:
  stage: test
  variables:
    TEST_NAME: "ltl2ldba-vanilla"
  <<: *ltlcross_template

ltl2ldba generalised-Büchi:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2ldba-genb"
  <<: *ltlcross_template

ltl2ldba generalised-Büchi Non-Det:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2ldba-genb-nondet"
  <<: *ltlcross_template

ltl2ldba generalised-Büchi Vanilla:
  stage: test
  variables:
    TEST_NAME: "ltl2ldba-genb-vanilla"
  <<: *ltlcross_template

## Breakpoint-Free

ltl2ldba Büchi Breakpoint-Free:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2ldba-bpfree"
  <<: *ltlcross_template

ltl2ldba Büchi Breakpoint-Free Non-Det:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2ldba-bpfree-nondet"
  <<: *ltlcross_template

ltl2ldba Büchi Breakpoint-Free Vanilla:
  stage: test
  variables:
    TEST_NAME: "ltl2ldba-bpfree-vanilla"
  <<: *ltlcross_template

ltl2ldba generalised-Büchi Breakpoint-Free:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2ldba-genb-bpfree"
  <<: *ltlcross_template

ltl2ldba generalised-Büchi Breakpoint-Free Non-Det:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2ldba-genb-bpfree-nondet"
  <<: *ltlcross_template

ltl2ldba generalised-Büchi Breakpoint-Free Vanilla:
  stage: test
  variables:
    TEST_NAME: "ltl2ldba-genb-bpfree-vanilla"
  <<: *ltlcross_template

# NBA to LDBA translations

nba2ldba:
  stage: test
  variables:
    TEST_NAME: "nba2ldba"
  <<: *ltlcross_template

# LTL to DPA translations

ltl2dpa:
  stage: test-optimizations
  variables:
    TEST_NAME: "ltl2dpa"
  <<: *ltlcross_template

ltl2dpa Vanilla:
  stage: test
  variables:
    TEST_NAME: "ltl2dpa-vanilla"
  <<: *ltlcross_template

ltl2dpa Parallel:
  stage: test-parallel-translation
  variables:
    TEST_NAME: "ltl2dpa-parallel"
  <<: *ltlcross_template

# LTL to D*A translations

ltl2da Parallel:
  stage: test-parallel-translation
  variables:
    TEST_NAME: "ltl2da-parallel"
  <<: *ltlcross_template

# LTL to DELA translations

delag Strict:
  stage: test
  variables:
    TEST_NAME: "delag-strict"
  <<: *ltlcross_template

delag:
  stage: test
  variables:
    TEST_NAME: "delag"
  <<: *ltlcross_template

delag Fallback:
  stage: test
  variables:
    TEST_NAME: "delag-fallback"
  <<: *ltlcross_template

# Benchmarks

bench ltl2ldba:
  variables:
    BENCH_NAME: "ltl2ldba"
  <<: *benchmark_template

bench delag:
  variables:
    BENCH_NAME: "delag"
  <<: *benchmark_template

bench ltl2dpa:
  variables:
    BENCH_NAME: "ltl2dpa"
  <<: *benchmark_template

bench ltl2da:
  variables:
    BENCH_NAME: "ltl2da"
  <<: *benchmark_template

bench ltl2dpa Parallel:
  variables:
    BENCH_NAME: "ltl2dpa-parallel"
  <<: *benchmark_template

bench ltl2da Parallel:
  variables:
    BENCH_NAME: "ltl2da-parallel"
  <<: *benchmark_template