<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         name="Custom ruleset"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_2_0_0.xsd">
    <rule ref="rulesets/java/basic.xml"/>
    <rule ref="rulesets/java/braces.xml"/>
    <rule ref="rulesets/java/strings.xml">
        <!-- We often use logging, building log messages sometimes involves duplicate literals -->
        <exclude name="AvoidDuplicateLiterals"/>
    </rule>
    <rule ref="rulesets/java/design.xml">
        <exclude name="UseVarargs"/>
        <exclude name="ConfusingTernary"/>
    </rule>
    <rule ref="rulesets/java/unusedcode.xml"/>
    <rule ref="rulesets/java/finalizers.xml"/>
    <rule ref="rulesets/java/controversial.xml">
        <!-- Always call super() in constructors -->
        <exclude name="CallSuperInConstructor"/>
        <!-- No package-visibility -->
        <exclude name="DefaultPackage"/>
        <!-- Methods should only have one return -->
        <exclude name="OnlyOneReturn"/>
        <!-- Dataflow analysis (referencing potentially undefined variables etc.) -->
        <exclude name="DataflowAnomalyAnalysis"/>
        <!-- No magic numbers -->
        <exclude name="AvoidLiteralsInIfCondition"/>
        <!-- No final locals -->
        <exclude name="AvoidFinalLocalVariable"/>
        <!-- Always use ConcurrentHashMap instead of hash map - reduces performance if not
          multi-threaded -->
        <exclude name="UseConcurrentHashMap"/>
        <!-- Use native - Needed for sylvan -->
        <exclude name="AvoidUsingNativeCode"/>
        <!-- Each class should have at least one constructor -->
        <exclude name="AtLeastOneConstructor"/>
        <!-- Assigning a variable to null -->
        <exclude name="NullAssignment" />
    </rule>
    <rule ref="rulesets/java/junit.xml">
        <exclude name="JUnitTestContainsTooManyAsserts"/>
    </rule>
    <rule ref="rulesets/java/imports.xml">
        <exclude name="TooManyStaticImports"/>
    </rule>
    <rule ref="rulesets/java/strictexception.xml"/>
    <rule ref="rulesets/java/optimizations.xml">
        <exclude name="LocalVariableCouldBeFinal"/>
        <exclude name="RedundantFieldInitializer"/>
        <exclude name="MethodArgumentCouldBeFinal"/>
        <!-- Don't use new Foo() in loops - often is unavoidable -->
        <exclude name="AvoidInstantiatingObjectsInLoops" />
    </rule>

    <!-- <rule ref="rulesets/java/codesize.xml/CyclomaticComplexity">
        <properties>
            <property name="reportLevel" value="5"/>
        </properties>
    </rule>
    -->
    <exclude-pattern>.*/owl/grammar/.*</exclude-pattern>
</ruleset>
